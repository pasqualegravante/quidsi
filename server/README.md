# QUIDSI Server

## Come eseguire
Per eseguire il server vedi prima il README del client.
Comandi da lanciare sul terminale:
`npm install`   - serve per installare le dipendenze utilizzate in node
`npm start`     - serve per eseguire un'istanza del server su localhost
# QUIDSI Client

## Come eseguire
Comandi da lanciare sul terminale:
`npm install`   - serve per installare le dipendenze utilizzate in node
`npm run build` - crea la cartella dist contenente i file statici html, ..., da servire tramite server
`npm run dev`   - utile al testing unicamente del frontend